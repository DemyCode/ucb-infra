version: "3.9"
services:
  api:
    image: ucb-api
    build: ./ucb-api
    ports:
      - "8000:8000"
    command: "uvicorn app.main:app --host 0.0.0.0 --port 8000"
  postgresql:
    image: postgres
